{
  "name": "canopy-scripts",
  "private": true,
  "version": "0.0.0",
  "description": "Self-contained TS test harness for canopy plugin scripts (canopy-web-pat-mint). Independent of canopy's Python tooling.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2",
    "vitest": "^4.1.3"
  }
}
